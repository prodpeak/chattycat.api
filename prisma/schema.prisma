datasource db {
  provider = "cockroachdb"
  url      = "postgresql://chattycat:ixU3IijzJmjJP06oF7npEg@main-lamb-15477.7tt.aws-us-east-1.cockroachlabs.cloud:26257/defaultdb?sslmode=verify-full"
}

generator client {
  provider = "prisma-client-js"
}

model Chat {
  id         Int      @id @default(sequence())
  text       String
  seen       Boolean  @default(false)
  created_at DateTime @default(now())
}
